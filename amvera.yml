meta:
  environment: node
  version: 20

build:
  commands:
    - cd backend
    - npm install
    - npm run build

run:
  command: cd backend && npm start
  port: 5000

tasks:
  - name: db-migrate
    command: cd backend && npm run db:init
    runOn: deploy